\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{natbib}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{enumitem}
\usepackage{subcaption}

\title{When Modalities Disagree: Failure Modes and Mitigations\\in Multimodal Sentiment Classification}
\author{
  Krishna \\
  Department of Statistics \& Applied Probability \\
  University of California, Santa Barbara \\
  \texttt{krish@ucsb.edu}
}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Multimodal learning integrates diverse data sources under the assumption that combining modalities improves prediction.
However, real-world data frequently contains conflicting signals across modalities.
We investigate how multimodal sentiment classifiers behave when input modalities disagree, using the Amazon Reviews 2023 dataset across multiple product categories.
We construct binary sentiment models using text (sentence-transformer embeddings), structured metadata, and three fusion architectures (early, late, and gated), then systematically evaluate performance across agreement and disagreement subsets.
Our analysis reveals that: (1) fusion improves average accuracy (88.8\% vs.\ 86.8\% text-only) but degrades severely under conflict (64.1\% on disagreement cases); (2) the fusion model follows text predictions in 82.2\% of true-conflict cases; (3) disagreement induces 13$\times$ worse calibration (ECE of 0.219 vs.\ 0.017) that persists after temperature scaling; and (4) all three architectures perform comparably, suggesting a paradigm limitation.
We then show that \textbf{disagreement-aware loss reweighting} improves disagreement accuracy by 3.2~pp and F1 by 4.3~pp with only 1.5~pp overall cost, demonstrating the problem is addressable.
All comparisons include bootstrap 95\% confidence intervals and McNemar's tests.
\end{abstract}

\section{Introduction}

Multimodal machine learning combines heterogeneous data sources---text, images, structured metadata, and other signals---to improve predictions \citep{baltrusaitis2019multimodal}.
A common assumption is that more modalities always help: more information should yield better decisions.
In practice, however, modalities can conflict.
A product review's text may sound enthusiastic while its star rating is low, or metadata features (price, helpfulness votes) may suggest a pattern inconsistent with the textual content.

When modalities disagree, fusion models face a fundamental dilemma: which signal should dominate?
Prior work has shown that multimodal models can over-rely on a single modality \citep{wang2020makes, peng2022balanced}, and that modality gaps in representation space can hinder effective fusion \citep{liang2022mind}.
Yet few studies systematically characterize \emph{when} fusion models fail, \emph{how} they resolve conflicting inputs, and whether simple interventions can help.

This paper makes the following contributions:
\begin{enumerate}[leftmargin=*,nosep]
  \item We define a disagreement severity framework based on text--rating conflict, creating four evaluation subgroups (agreement, weak/medium/strong disagreement).
  \item We compare text-only, metadata-only, and three multimodal fusion architectures across these subgroups with full statistical rigor.
  \item We introduce probability-based and true-conflict dominance analyses that quantify modality reliance beyond binary labels.
  \item We show that disagreement-aware loss reweighting substantially improves disagreement-subset performance, demonstrating the problem is mitigable.
  \item We provide group-conditional calibration analysis showing that standard temperature scaling fails under modality conflict.
  \item We present a heuristic error taxonomy identifying mixed sentiment, sarcasm, and short reviews as primary failure drivers.
\end{enumerate}

\section{Related Work}

\paragraph{Multimodal Learning.}
\citet{baltrusaitis2019multimodal} survey multimodal ML, categorizing fusion into early, late, and hybrid approaches.
Recent work has moved toward attention-based fusion \citep{huang2021makes}, but how fusion handles conflicting inputs remains underexplored.

\paragraph{Modality Dominance and Bias.}
\citet{wang2020makes} show that multimodal networks often converge to relying on one modality due to learning dynamics.
\citet{peng2022balanced} propose gradient modulation to address this.
In VQA, language bias causes models to ignore visual evidence \citep{goyal2017making}.
Our work quantifies dominance specifically under modality conflict.

\paragraph{Modality Gap.}
\citet{liang2022mind} identify a ``modality gap'' where different modalities occupy distinct embedding regions, hindering integration under conflicting signals.

\paragraph{Calibration.}
\citet{guo2017calibration} show that modern networks are poorly calibrated.
\citet{nixon2019measuring} formalize ECE.
We extend calibration analysis to agreement/disagreement subgroups.

\paragraph{Sentence Representations.}
We use sentence-transformer embeddings \citep{reimers2019sentence} as our text representation, which provide dense 384-dimensional vectors encoding semantic similarity.

\paragraph{Robustness and Failure Analysis.}
Subgroup evaluation is standard for fairness \citep{barocas2019fairness}; our application to multimodal conflict analysis is novel.

\section{Problem Formulation}

Let $\mathbf{x} = (\mathbf{x}^{(t)}, \mathbf{x}^{(m)})$ denote an input with text modality $\mathbf{x}^{(t)}$ and metadata modality $\mathbf{x}^{(m)}$, with true label $y \in \{0, 1\}$.

We train three classifiers: text-only $f_t$, metadata-only $f_m$, and multimodal $f_{mm}$.
We define \textbf{modality disagreement} using an external sentiment classifier $g$: when $g(\mathbf{x}^{(t)}) \neq y$, text sentiment contradicts the rating-derived label.
We stratify by $g$'s confidence $c_g$ into weak ($c_g < 0.70$), medium ($0.70 \leq c_g < 0.90$), and strong ($c_g \geq 0.90$) disagreement.

\paragraph{Probability-Based Dominance.}
For each sample, we compute:
\begin{equation}
  r = \frac{|p_{mm} - p_t|}{|p_{mm} - p_t| + |p_{mm} - p_m| + \epsilon}
\end{equation}
where $p_{mm}$, $p_t$, $p_m$ are predicted $P(y=1)$.
$r \approx 0$ indicates text dominance; $r \approx 1$ indicates metadata dominance.

\section{Data}

We use the Amazon Reviews 2023 dataset \citep{hou2024bridging}, constructing binary sentiment tasks.
Reviews with rating $\geq 4$ are positive; $\leq 2$ are negative; neutrals are excluded.

\paragraph{Class Balancing.}
The raw dataset is 77.5\% positive.
We downsample the majority class to match the minority, yielding 20{,}674 balanced samples (10{,}337 per class).
This ensures the majority-class baseline achieves exactly 50\% accuracy.

\paragraph{Modalities.}
The \textbf{text modality} is encoded via \texttt{all-MiniLM-L6-v2} sentence-transformer \citep{reimers2019sentence} into 384 dimensions.
The \textbf{metadata modality} comprises seven features: review length (words/characters), helpful votes, verified purchase, product average rating, product rating count, and review year.

\paragraph{Splits and Disagreement.}
We use 70/15/15 stratified splits (seed 42): 14{,}471 train, 3{,}101 val, 3{,}102 test.
A pretrained DistilBERT sentiment classifier labels 11.1\% of test samples as disagreement (343 of 3{,}102), with 27 weak, 50 medium, and 266 strong.

\section{Methods}

\subsection{Models}

\paragraph{Text-Only.}
Sentence-transformer embeddings fed into logistic regression ($C=1.0$).

\paragraph{Metadata-Only.}
XGBoost (200 estimators, max depth 6) on standardized features.

\paragraph{Multimodal Fusion.}
Three architectures:
\textbf{Early fusion} concatenates text (384-d) and metadata (8-d), then applies a 2-layer MLP (256$\to$64$\to$2) with ReLU and dropout (0.2).
\textbf{Late fusion} uses separate per-modality MLPs and averages logits.
\textbf{Gated fusion} learns a sigmoid gate over modality representations.
All trained for 15 epochs with Adam (lr=$10^{-3}$, batch size 64).
We use default hyperparameters throughout, as our focus is on comparing model behavior under disagreement rather than maximizing absolute performance.

\subsection{Disagreement-Aware Training}

Standard fusion training treats all samples equally, but disagreement samples represent qualitatively different inputs.
We propose \textbf{disagreement-aware loss reweighting}: during training, disagreement samples receive weight $w > 1$ in the cross-entropy loss:
\begin{equation}
  \mathcal{L} = \frac{1}{N} \sum_{i=1}^{N} w_i \cdot \ell(f_{mm}(\mathbf{x}_i), y_i), \quad w_i = \begin{cases} w & \text{if disagreement} \\ 1 & \text{otherwise} \end{cases}
\end{equation}
This encourages the model to pay more attention to conflict cases without modifying the architecture.
We evaluate $w \in \{3, 5\}$.

\subsection{Evaluation}

\paragraph{Metrics.}
Accuracy, F1, AUROC, ECE, and Brier score, all with 95\% bootstrap CIs (1{,}000 resamples).

\paragraph{Statistical Tests.}
McNemar's test (continuity-corrected) for pairwise comparisons.

\paragraph{Calibration.}
ECE overall and by agreement group.
Temperature scaling learned on validation data.

\paragraph{Selective Prediction.}
Accuracy--coverage curves at varying confidence thresholds (Figure~\ref{fig:selective}).

\section{Results}

\subsection{Overall Performance}

Table~\ref{tab:main} reports test-set metrics.
The multimodal model achieves 0.888 accuracy (0.877--0.899), outperforming text-only at 0.868 (0.855--0.879).
McNemar's test confirms significance ($\chi^2 = 17.6$, $p < 0.001$).

On the disagreement subset ($n = 343$), the multimodal model's accuracy drops to 0.641 (0.589--0.691)---wide CIs reflecting the smaller sample.
Disagreement AUROC is 0.719 (0.659--0.771), substantially below the overall 0.956.

\begin{table}[htbp]
\centering
\small
\caption{Test-set performance with 95\% bootstrap CIs ($n = 3{,}102$; disagreement $n = 343$).}
\label{tab:main}
\begin{tabular}{llcccc}
\toprule
\textbf{Model} & \textbf{Subset} & \textbf{Accuracy} & \textbf{F1} & \textbf{AUROC} & \textbf{ECE} \\
\midrule
Majority & Overall & .500 & .000 & .500 & --- \\
\midrule
\multirow{2}{*}{Text-Only} & Overall & .868 (.855--.879) & .866 (.852--.879) & .943 (.935--.950) & .028 \\
 & Disagree & .592 (.536--.647) & .648 (.587--.703) & .631 (.564--.690) & .220 \\
\midrule
\multirow{2}{*}{Metadata} & Overall & .679 (.663--.695) & .680 (.661--.697) & .749 (.732--.765) & .035 \\
 & Disagree & .615 (.566--.668) & .670 (.617--.719) & .671 (.609--.728) & .159 \\
\midrule
\multirow{2}{*}{Multimodal} & Overall & .888 (.877--.899) & .886 (.874--.898) & .956 (.949--.962) & .023 \\
 & Disagree & .641 (.589--.691) & .685 (.630--.736) & .719 (.659--.771) & .219 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Performance by Agreement Group}

Table~\ref{tab:groups} and Figure~\ref{fig:severity} show dramatic degradation.
On agreement cases, the multimodal model achieves 91.9\%; on strong disagreement ($n = 266$), it drops to 58.6\%---\emph{worse} than metadata-only (61.3\%).
Note that the weak ($n = 27$) and medium ($n = 50$) subgroups have small samples and should be interpreted cautiously.

\begin{table}[htbp]
\centering
\caption{Model accuracy by agreement group. Weak/medium groups have small $n$ and wide CIs.}
\label{tab:groups}
\begin{tabular}{lcccccc}
\toprule
\textbf{Model} & \textbf{Overall} & \textbf{Agreement} & \textbf{Weak} & \textbf{Medium} & \textbf{Strong} & \textbf{All Disagree} \\
 & $(3{,}102)$ & $(2{,}759)$ & $(27)$ & $(50)$ & $(266)$ & $(343)$ \\
\midrule
Text-Only     & .868 & .902 & .704 & .780 & .545 & .592 \\
Metadata-Only & .679 & .686 & .630 & .620 & .613 & .615 \\
Multimodal    & .888 & .919 & .815 & .840 & .586 & .641 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{disagreement_severity_trend.png}
\caption{Error rate by disagreement severity. The multimodal model shows the steepest decline from agreement (8.1\% error) to strong disagreement (41.4\% error).}
\label{fig:severity}
\end{figure}

\subsection{Fusion Architecture Comparison}

Table~\ref{tab:fusion} shows remarkably similar performance across all three architectures (88.3--88.9\% overall, 63.0--65.0\% disagree), suggesting modality conflict is a paradigm limitation, not an architectural one.

\begin{table}[htbp]
\centering
\caption{Fusion architecture comparison.}
\label{tab:fusion}
\begin{tabular}{lccccc}
\toprule
\textbf{Fusion} & \textbf{Accuracy} & \textbf{F1} & \textbf{AUROC} & \textbf{Disagree Acc.} & \textbf{Disagree F1} \\
\midrule
Early  & .888 & .886 & .956 & .641 & .685 \\
Late   & .889 & .888 & .956 & .650 & .700 \\
Gated  & .883 & .883 & .957 & .630 & .683 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Modality Dominance}

In the 1{,}098 cases where text and metadata models disagree, the multimodal model follows text in 82.2\% of cases.
The probability-based dominance ratio (Figure~\ref{fig:prob_dom}) confirms this: mean ratio $= 0.380$, with 69.4\% text-leaning.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.7\textwidth]{probability_dominance_histogram.png}
\caption{Dominance ratio distribution in disagreement cases. Left-skew confirms text dominance.}
\label{fig:prob_dom}
\end{figure}

\subsection{Calibration Analysis}

Table~\ref{tab:calibration} reveals a 13$\times$ ECE gap: the multimodal model is well-calibrated on agreement (0.017) but severely miscalibrated on disagreement (0.219).
Temperature scaling provides negligible improvement on disagreement (0.219 $\to$ 0.215) and worsens text model calibration (0.220 $\to$ 0.253), confirming that a single global temperature cannot address this distributional shift.

\begin{table}[htbp]
\centering
\caption{Group-conditional ECE before and after temperature scaling.}
\label{tab:calibration}
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Group} & \textbf{ECE (Before)} & \textbf{ECE (After)} & \textbf{Temp.} \\
\midrule
\multirow{2}{*}{Text-Only} & Agreement & 0.052 & 0.024 & \multirow{2}{*}{0.662} \\
 & Disagreement & 0.220 & 0.253 & \\
\midrule
\multirow{2}{*}{Metadata} & Agreement & 0.035 & 0.024 & \multirow{2}{*}{1.311} \\
 & Disagreement & 0.159 & 0.151 & \\
\midrule
\multirow{2}{*}{Multimodal} & Agreement & 0.017 & 0.024 & \multirow{2}{*}{1.153} \\
 & Disagreement & 0.219 & 0.215 & \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{selective_prediction.png}
\caption{Selective prediction: accuracy vs.\ coverage at varying confidence thresholds. Abstaining on low-confidence predictions helps more for disagreement cases but cannot close the gap.}
\label{fig:selective}
\end{figure}

\subsection{Statistical Significance}

On the overall test set, all pairwise differences are significant (Table~\ref{tab:mcnemar}).
On the disagreement subset, multimodal vs.\ text-only is significant ($p = 0.031$), but metadata vs.\ multimodal ($p = 0.456$) and text vs.\ metadata ($p = 0.568$) are not---model differences largely vanish under conflict.

\begin{table}[htbp]
\centering
\caption{McNemar's test (continuity-corrected).}
\label{tab:mcnemar}
\begin{tabular}{llccl}
\toprule
\textbf{Comparison} & \textbf{Subset} & $\chi^2$ & $p$ & \textbf{Sig.} \\
\midrule
Text vs.\ Multimodal    & Overall      & 17.6 & ${<}0.001$ & Yes \\
Text vs.\ Multimodal    & Disagreement & 4.65 & 0.031 & Yes \\
Meta vs.\ Multimodal    & Overall      & 451.0 & ${<}0.001$ & Yes \\
Meta vs.\ Multimodal    & Disagreement & 0.56 & 0.456 & No \\
Text vs.\ Meta          & Overall      & 311.7 & ${<}0.001$ & Yes \\
Text vs.\ Meta          & Disagreement & 0.33 & 0.568 & No \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Disagreement-Aware Training}

Table~\ref{tab:disagree_aware} shows that disagreement-aware loss reweighting substantially improves disagreement performance.
With $w = 5$, disagreement accuracy improves from 64.1\% to 67.3\% (+3.2~pp) and disagreement F1 from 0.685 to 0.728 (+4.3~pp), at a modest 1.5~pp cost to overall accuracy.
This demonstrates that the degradation under conflict is partially mitigable through training-time awareness, without any architectural change.

\begin{table}[htbp]
\centering
\caption{Disagreement-aware training comparison. Bold indicates best disagreement metric.}
\label{tab:disagree_aware}
\begin{tabular}{lcccccc}
\toprule
\textbf{Method} & \multicolumn{2}{c}{\textbf{Overall}} & \multicolumn{2}{c}{\textbf{Agreement}} & \multicolumn{2}{c}{\textbf{Disagreement}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
 & Acc. & F1 & Acc. & F1 & Acc. & F1 \\
\midrule
Standard           & .888 & .886 & .919 & .916 & .641 & .685 \\
DA ($w = 3$)       & .880 & .880 & .907 & .905 & .662 & .714 \\
DA ($w = 5$)       & .873 & .875 & .898 & .897 & \textbf{.673} & \textbf{.728} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Error Taxonomy}

Table~\ref{tab:errors} categorizes 200 multimodal errors via heuristic pattern matching.
Mixed-sentiment reviews (22.5\%) are the most identifiable category, followed by sarcasm/irony (11.5\%) and short reviews (8.5\%).
Short reviews have the highest disagreement rate (52.9\%), suggesting insufficient text amplifies modality conflict.
Conditional-sentiment reviews (``good for the price, but...'') and comparative reviews (``better than X'') each contribute 1.5--3.5\%.

\begin{table}[htbp]
\centering
\caption{Error taxonomy (200 multimodal errors, heuristic classification).}
\label{tab:errors}
\begin{tabular}{lccc}
\toprule
\textbf{Error Type} & \textbf{Count (\%)} & \textbf{Mean Conf.} & \textbf{\% Disagree} \\
\midrule
Mixed Sentiment      & 45 (22.5\%) & 0.724 & 28.9\% \\
Sarcasm/Irony        & 23 (11.5\%) & 0.749 & 47.8\% \\
Short Review         & 17 (8.5\%)  & 0.771 & 52.9\% \\
Rating Misuse        & 7 (3.5\%)   & 0.702 & 0.0\% \\
Conditional Sent.    & 7 (3.5\%)   & 0.831 & 57.1\% \\
Topic Drift          & 4 (2.0\%)   & 0.656 & 25.0\% \\
Comparative          & 3 (1.5\%)   & 0.890 & 100.0\% \\
Ambiguous            & 2 (1.0\%)   & 0.857 & 100.0\% \\
Other                & 92 (46.0\%) & 0.756 & 35.9\% \\
\bottomrule
\end{tabular}
\end{table}

\section{Discussion}

\paragraph{Does Multimodal Always Help?}
On average, yes---but the 27.8~pp accuracy drop from agreement to disagreement (91.9\% $\to$ 64.1\%) reveals that aggregate metrics mask important reliability failures.
On strong disagreement, the multimodal model (58.6\%) underperforms metadata-only (61.3\%), suggesting text dominance becomes harmful when text is misleading.

\paragraph{Fusion Architecture Does Not Matter.}
Early, late, and gated fusion achieve 88.3--88.9\% accuracy with nearly identical disagreement performance.
All learn similar text-dominant strategies.
This motivates conflict-aware training objectives over novel architectures.

\paragraph{Disagreement-Aware Training Works.}
The 3.2~pp improvement in disagreement accuracy from simple loss reweighting ($w=5$) demonstrates that the problem is partially mitigable.
The overall--disagreement trade-off (1.5~pp overall cost for 3.2~pp disagreement gain) is favorable in high-stakes applications where reliability under conflict matters more than average-case performance.

\paragraph{Calibration Under Conflict.}
The 13$\times$ ECE gap and temperature scaling's failure motivate group-conditional calibration.
In production, systems should flag high-confidence predictions in detected disagreement regions.

\paragraph{Practical Implications.}
We recommend:
(1) monitoring disagreement rates, as they predict reliability;
(2) applying disagreement-aware training when conflict cases matter;
(3) using selective prediction with higher thresholds for detected disagreement;
(4) developing modality-conditioned calibration methods.

\section{Limitations}

\begin{itemize}[nosep]
  \item Rating-derived labels are an imperfect sentiment proxy.
  \item The pretrained sentiment model for disagreement labeling may introduce systematic bias.
  \item Our metadata modality is limited to seven structured features; richer metadata (images, video) may behave differently.
  \item The error taxonomy uses heuristic patterns; manual annotation would strengthen it.
  \item Disagreement-aware training requires disagreement labels at training time; we do not address detection at inference time for unseen distributions.
\end{itemize}

\section{Conclusion}

We have shown that multimodal sentiment classifiers exhibit systematic failure modes under modality conflict.
On a class-balanced dataset ($n = 3{,}102$ test, 11.1\% disagreement), the multimodal model achieves 88.8\% overall accuracy but drops to 64.1\% under disagreement and 58.6\% on strong disagreement---worse than metadata-only.
Text dominance is pervasive (82.2\%), all three fusion architectures perform comparably, and standard calibration fails to address the 13$\times$ ECE gap.

Crucially, we demonstrate that disagreement-aware loss reweighting improves disagreement F1 by 4.3~pp, showing these failures are partially mitigable.
Future directions include:
(1) modality dropout and stochastic gating;
(2) uncertainty-based fusion with per-sample reliability estimates;
(3) modality-conditioned calibration;
and (4) disagreement detection at inference time without ground-truth labels.

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
